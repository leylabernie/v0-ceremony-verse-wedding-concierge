import type { ReactNode } from "react"
import { PlanningNextStep } from "@/components/planning-next-step"
export default function ResortsLayout({children}:{children:ReactNode}) { return <>{children}<PlanningNextStep /></> }
